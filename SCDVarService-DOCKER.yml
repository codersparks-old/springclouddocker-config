server:
  port: 8080

eureka:
  instance:
      hostname: ${SCD_DISCOVERY_INSTANCE_HOST}
      ip: ${SCD_DISCOVERY_INSTANCE_HOST}
      port: ${SCD_DISCOVERY_INSTANCE_PORT}

instance:
  var1: localVariable1
  var2: localVariable2